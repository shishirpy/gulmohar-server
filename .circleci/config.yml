version: 2

jobs:
  build:
    working_directory: ~/gulmohar-server
    docker:
      - image: continuumio/miniconda3
        environment:
          PIPENV_VENV_IN_PROJECT: true

        steps:
          - checkout # checkout source code to working directory
          - run:
              conda env create -f broker_ubuntu.yml
              conda activate mdbroker
              pytest



            

# version: 2.1

# orbs:
#   python: circleci/python@0.2.1

# jobs:
#   build-and-test:
#     executor: python/default
#     steps:
#       - checkout
#       - python/load-cache
#       - python/install-deps
#       - python/save-cache
#       - run:
#           command: ./manage.py test
#           name: Test

# workflows:
#   main:
#     jobs:
#       - build-and-test
